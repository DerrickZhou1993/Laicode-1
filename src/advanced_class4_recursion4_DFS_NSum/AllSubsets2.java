package advanced_class4_recursion4_DFS_NSum;
/**
 * 
 * @author @Yifeng
 * 
 * Q1.3 Given a set of characters represented by a String, return a list containing all subsets of the characters.

	Assumptions
	
	There could be duplicate characters in the original set.
	​Examples
	
	Set = "abc", all the subsets are ["", "a", "ab", "abc", "ac", "b", "bc", "c"]
	Set = "abb", all the subsets are ["", "a", "ab", "abb", "b", "bb"]
	Set = "", all the subsets are [""]
	Set = null, all the subsets are []
 */

/*
 * basic solution for DFS
 * 		1.how many layers?
 * 		2.what are the states of each layer? 
 */

/*
 * Q1.0 All Subset 1 problem (classical DFS) 
 */

/*
 * Q1.1 array of int and divide into two parts whose sum is equal? 
 */

/*
 * Q1.2 subset's size = k?
 */

/*
 * subset's size = k and with duplicate
 */
public class AllSubsets2 {

}
